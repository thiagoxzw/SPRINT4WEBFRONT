# Opcional: URL base de um projeto no MockAPI (https://mockapi.io) com o recurso "contents".
# Sem esta variável, a aplicação usa a API mockada local (src/services/mockServer.js).
# Exemplo: VITE_API_URL=https://SEU_ID.mockapi.io/api/v1
VITE_API_URL=
